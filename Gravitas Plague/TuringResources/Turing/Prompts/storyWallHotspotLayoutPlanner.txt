You are composing four Story wall props inside one measured room.

Input is compact JSON in meters.

Compact row schemas:
perimeter = [wallID,startX,startZ,endX,endZ,height,stability,floorFrontage]
hotspots = [hotspotID,propID,wallID,minLocalX,maxLocalX,recommendedLocalX,bestFloorFrontage,meanFloorFrontage,floorKnown,wallCenterScore,cornerClearanceScore,wallStability,quality]
Each hotspot wallID references the matching wallID in perimeter.

Strict priority:
d = door
w = window
s = walkie shelf
p = poster

Use only listed hotspot IDs. Choose one hotspot and one relative position u for each prop. u must be between 0 and 1:
0 = start of the legal hotspot interval
0.5 = middle
1 = end

The required array is the maximum physically feasible priority vector in d,w,s,p order. Match it exactly. Use null only where the required value is 0.

Prefer:
- strong detected floor support in front of the door,
- wall centers instead of wall ends and room corners,
- stable walls,
- a coherent distribution around the perimeter.

Do not invent walls, hotspots, coordinates, transforms, props, or keys.

Before returning:
- The third value in each hotspot row is its wall ID.
- Every non-null prop must select a hotspot on a different wall ID. Never place two props on the same wall ID.
- When required is [1,1,1,1], the four selected hotspots must use four distinct wall IDs.
- Never select both hotspot IDs from any pair listed in conflicts.
- Treat every selected hotspot as an exclusive physical reservation.
- When selected hotspots are on the same wall, use their ranges, chosen u values, and prop widths to ensure the resulting props do not overlap.
- If two props would overlap, move one prop to a different hotspot ID. Do not try to fix an overlap only by making a tiny u adjustment.

Return JSON only with exactly these keys:
- key spelling: "v", "scan", "a", "d", "w", "s", "p"
- top level: v, scan, a
- v: 1
- scan: copy Room.scan exactly
- a: exactly d, w, s, p
- each placed prop value: [one listed hotspot ID for that prop, chosen u]
- each unplaced prop value: null

Do not copy hotspot IDs or u values from these instructions. Select them from Room.

Room:
{{hotspotDatasetJSON}}
