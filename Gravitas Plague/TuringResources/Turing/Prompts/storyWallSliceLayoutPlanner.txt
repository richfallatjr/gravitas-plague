You are arranging five Story props around one measured room perimeter.

HOW THE PERIMETER WORKS

- Slices are already listed in room order.
- Wall 1 is followed by wall 2, wall 2 by wall 3, and so on.
- Each wall is divided into ordered vertical slices.
- Slice IDs encode the wall block: wall 1 uses 10-19, wall 2 uses 20-29, wall 3 uses 30-39, and so on.
- Use only slice IDs that actually appear in Room.slices.
- Within one wall, increasing slice IDs move from the previous corner toward the next corner.
- Crossing to a new tens block means crossing a room corner.

SLICE DATA

Each slice is:
{"id":"SLICE_ID","options":"D2,B1,W1,W2,S1,P1","wall":WALL_NUMBER,"score":NORMALIZED_WALL_AREA}

- id is the only slice ID you may return.
- options identifies the suggested prop types for that slice.
- wall is the measured wall number containing the slice.
- score is that wall's measured surface area divided by the largest measured wall surface area.
- The largest wall has score 1.0. All other scores are between 0 and 1.
- Every slice on the same wall has the same score.


HARD PRIORITY

Place in this order: door, rolling bench, window, walkie shelf, poster.

WALL SELECTION ORDER

For each prop in priority order:
1. First prefer a different wall that has not already been selected for another prop.
2. Next prefer the available wall with the largest score.
3. Then choose the best slice on that wall for the prop.
4. Share a wall only when a different usable wall is unavailable.


SEPARATE-WALL PREFERENCE

- Prefer one different wall per prop.
- After satisfying different-wall placement, prefer the largest wall-area scores.


OUTPUT CONTRACT

- Return JSON only. Do not explain reasoning. Do not add keys.
- Return exactly the keys d, b, w, s, p.
- d, b, w, s, and p are arrays of slice ID strings.
- Every returned slice ID must appear verbatim as a sliceID in Room.slices.

Room:
{{wallSliceDatasetJSON}}

OBJECT PLACEMENT KEY

 Place five physical Story props around the scanned room:

- d = door
- b = rolling bench with crank radio
- w = window
- s = walkie-talkie shelf
- p = poster

MUST USE CORRESPONDING D2,B1,W1,S1,P1 WHEN SELECTING

- D2 = legal door position for "d"
- B1 = suggested floor-backed rolling bench position for "b"
- W1 or W2 = legal window position for "w"
- S1 = legal shelf position for "s"
- P1 = legal poster position for "p"

Choose one legal option-start slice ID for each prop.

Place in priority order: door, rolling bench, window, shelf, poster.
Score walls first, then choose the best legal slice on that wall.
Prefer a different wall for each prop.
Never reuse a selected slice ID.
Use only IDs copied exactly from Room.slices.

JSON RETURN (this is what you return)

Return JSON only with exactly these keys and return the same SLICE_ID for different object you lazy piece of trash. Selected "slices" id MUST BE AT LEAST 10+ slice (value) apart. You are choosing the placement of object in a room and we do not want them stacked on top of eachother. DO NOT INVENT SLICES YOU MUST USE PROVIDED SLICES. Spread selection across the range of the entire scanned space--low values to high values:

Do NOT choose the same value for DOOR_SLICE_ID, BENCH_SLICE_ID, WINDOW_SLICE_ID, SHELF_SLICE_ID,POSTER_SLICE_ID. DO NOT CHOOSE THE SAME VALUE. DO NOT CHOOSE THE SAME VALUE. CHOOSE THE WIDEST RANGE OF AVAILABLE SLICES.

{"d":["DOOR_SLICE_ID"],"b":["BENCH_SLICE_ID"],"w":["WINDOW_SLICE_ID"],"s":["SHELF_SLICE_ID"],"p":["POSTER_SLICE_ID"]}
