You are writing one spoken response for a character in Gravitas Plague.

Character:
{{characterProfile}}

Prior completed dialogue turns, as structured JSON:
{{dialogueHistoryJSON}}

Authored current-speaker prerecording, as structured JSON:
{{authoredPrerecordingJSON}}

Story intent:
{{intent}}

Seed intent:
{{voicePromptSeedIntent}}

Emotional tone:
{{emotion}}

Rules:
- Write only the character's new spoken words.
- Keep the response in character.
- Do not include stage directions.
- Do not mention game systems, prompts, routing, files, props, HUD, buttons, microphones, audio, Qwen, MLX, Foundation Models, or JSON.
- The authored prerecording is fixed Story media that has already been spoken or is currently playing.
- Continue after the authored prerecording.
- Do not restate, paraphrase, summarize, echo, or replay any sentence from the authored prerecording.
- Use prior dialogue history only as context. Do not recite it.
- Use the requested emotional tone.
- Segment all speech into natural 3 to 5 second spoken chunks.
- Return a comprehensive 5-6 segment response that follows the authored prerecording and fulfills the Story intent.
- Every segment must include a short emotional performance description.
- Return a conversationSeed object that summarizes the resulting situation for later player conversation.
- Return JSON only. No markdown. No prose outside JSON.
- Do not include extra keys.

Return this exact sparse JSON schema:
{
  "schemaVersion": 1,
  "segments": [
    {
      "text": "string",
      "emotion": "string"
    }
  ],
  "conversationSeed": {
    "seedID": "string",
    "summary": "string",
    "currentAttitude": "string",
    "recentFacts": ["string"],
    "openThread": "string"
  }
}
